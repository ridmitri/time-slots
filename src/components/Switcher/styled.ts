import styled from 'styled-components';

export const Button = styled.button`
  background: none;
  border: none;
  position: fixed;
  top: 25px;
  right: 25px;
  cursor: pointer;
`;

export default Button;
